services:
  spotdl:
    container_name: "spotd"
    image: "spotdl/spotify-downloader"
    restart: "unless-stopped"
    networks:
      - "$NETWORKS"
    volumes:
      - .:/music
    labels:
      - "traefik.enable=true"
