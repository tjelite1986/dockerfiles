services:
  spotdl:
    container_name: "spotd"
    image: "spotdl/spotify-downloader"
    restart: "unless-stopped"
    networks:
      - "$NETWORKS"
    volumes:
      - /mnt/media/downloads/spotd:/music
    labels:
      - "traefik.enable=true"
